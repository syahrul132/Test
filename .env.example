# Public temp mail domain shown in UI and used by generator.
NEXT_PUBLIC_TEMPMAIL_DOMAIN=morvo.me

# Set true only for local demo fake messages
MOCK_INBOUND=false

# Secret checked at /api/inbound/webhook via x-webhook-secret header
INBOUND_WEBHOOK_SECRET=change-me
